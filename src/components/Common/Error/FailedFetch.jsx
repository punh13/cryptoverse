import React from "react";

export default function FailedFetch() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        textAlign: "center",
      }}
    >
      Failed to fetch data
    </div>
  );
}
